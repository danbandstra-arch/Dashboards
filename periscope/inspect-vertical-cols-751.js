const fs=require('fs'); const p='C:/Users/Dan Bandstra/Downloads/SCMPERISCOPEResults751.csv';
function parseCsv(text){const rows=[];let row=[],v='',q=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(c==='"'&&q&&n==='"'){v+='"';i++;}else if(c==='"'){q=!q;}else if(c===','&&!q){row.push(v);v='';}else if((c==='\n'||c==='\r')&&!q){if(c==='\r'&&n==='\n')i++;row.push(v);if(row.some(x=>String(x).trim()))rows.push(row);row=[];v='';}else v+=c;}row.push(v);if(row.some(x=>String(x).trim()))rows.push(row);return rows;}
const rows=parseCsv(fs.readFileSync(p,'utf8')); const hi=rows.findIndex(r=>r.includes('Internal ID')&&r.includes('Request Type')); const h=rows[hi]; const data=rows.slice(hi+1); [52,53,54,55,56].forEach(i=>{const m=new Map(); for(const r of data){const v=(r[i]||'(blank)').trim()||'(blank)'; m.set(v,(m.get(v)||0)+1)} console.log('\nCOL',i+1,h[i]); console.log([...m.entries()].sort((a,b)=>b[1]-a[1]).slice(0,12));});
